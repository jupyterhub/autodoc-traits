# This is a GitHub workflow defining a set of jobs with a set of steps.
# ref: https://docs.github.com/en/actions/learn-github-actions/workflow-syntax-for-github-actions
#
name: Test

on:
  pull_request:
    paths-ignore:
      - "**.md"
      - ".github/workflows/*"
      - "!.github/workflows/test.yaml"
  push:
    paths-ignore:
      - "**.md"
      - ".github/workflows/*"
      - "!.github/workflows/test.yaml"
    branches-ignore:
      - "dependabot/**"
      - "pre-commit-ci-update-config"
    tags:
      - "**"
  workflow_dispatch:

jobs:
  pytest:
    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
      matrix:
        include:
          - python-version: "3.10"
            sphinx-version: "6.*"
            traitlets-version: "4.*"
          - python-version: "3.11"
            sphinx-version: "7.*"
            traitlets-version: "4.*"
          - python-version: "3.12"
            sphinx-version: "8.*"
            traitlets-version: "5.*"
          - python-version: "3.13"
            sphinx-version: "9.*"
            traitlets-version: ""
          - python-version: "3.14"

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "${{ matrix.python-version }}"

      - name: create constraints
        run: |
          touch constraints.txt
          if [[ ! -z "${{ matrix.sphinx-version }}" ]]; then
            echo "sphinx==${{ matrix.sphinx-version }}" >> constraints.txt
          fi
          if [[ ! -z "${{ matrix.traitlets-version }}" ]]; then
            echo "traitlets==${{ matrix.traitlets-version }}" >> constraints.txt
          fi

      - run: pip install ".[test]" --constraint ./constraints.txt ${{ matrix.pip-install-addition }}

      - run: pytest
